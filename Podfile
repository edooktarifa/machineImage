platform :ios, '10.0'

target 'ImageMachine' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'RxSwift', '~> 4.0'
  pod 'RxCocoa', '~> 4.0'
  pod 'SnapKit'
  pod 'DKImagePickerController'
  
  # Pods for ImageMachine
  
  target 'ImageMachineTests' do
    inherit! :search_paths
    # Pods for testing
  end
  
  target 'ImageMachineUITests' do
    # Pods for testing
  end
  
end
